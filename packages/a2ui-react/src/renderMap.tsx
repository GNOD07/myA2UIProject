import type { RenderMap } from "@a2ui/core";
import { Text, Image, Icon, Video, Column, Row, List, Button, Card, TextField, Tabs } from "./components";

/**
 * A2UI 默认组件渲染映射表
 */
export const defaultRenderMap: RenderMap = {
  Text,
  Image,
  Icon,
  Video,
  Column,
  Row,
  List,
  Button,
  Card,
  TextField,
  Tabs,
};
